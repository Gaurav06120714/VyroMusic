{
  "name": "vyro-music",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "concurrently \"PORT=3006 npm run dev --workspace=apps/api\" \"npm run dev --workspace=apps/web\"",
    "dev:vyro": "concurrently \"PORT=3006 npm run dev --workspace=apps/api\" \"npm run dev --workspace=apps/web\" && sleep 4 && node open-in-vyro.mjs http://localhost:3005",
    "build": "npm run build --workspace=packages/types && npm run build --workspace=apps/api && npm run build --workspace=apps/web",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:web": "npm run dev --workspace=apps/web"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
